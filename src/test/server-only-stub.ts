/**
 * Stand-in for the `server-only` package under Vitest.
 *
 * See the alias in `vitest.config.ts` for why. Importing this does nothing;
 * the real guard still applies during `next build`.
 */
export {};
